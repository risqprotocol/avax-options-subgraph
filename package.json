{
  "name": "avaxoptions",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ risqprotocol/avax-options-subgraph",
    "create-local": "graph create --node http://localhost:8020/ risqprotocol/avax-options-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ risqprotocol/avax-options-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 risqprotocol/avax-options-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
